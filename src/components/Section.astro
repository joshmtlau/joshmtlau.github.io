---
const { color } = Astro.props;
const colorDirective = color ? "bg-" + color : "bg-snow";
---

<div class={colorDirective}>
  <section class="max-w-6xl mx-auto px-4 pt-12 pb-12">
    <slot />
  </section>
</div>
